{
  "name": "college-complaint-portal",
  "version": "1.0.0",
  "type": "module",
  "engines": { "node": "20.x" },
  "scripts": {
    "build": "npm ci && npm --prefix frontend ci && npm --prefix frontend run build && npm --prefix backend ci",
    "start": "node backend/index.js",
    "dev": "npm --prefix backend run dev"
  }
}
